name: Generate Contribution Snake

on:
  schedule:
    - cron: "0 2 * * *"   # nightly at 02:00 UTC
  workflow_dispatch: {}
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake SVGs
        uses: Platane/snk@v3
        with:
          github_user_name: PankajChauhanji
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Force jsDelivr CDN to pick up the new SVGs immediately
        run: |
          curl -s "https://purge.jsdelivr.net/gh/PankajChauhanji/PankajChauhanji@output/github-contribution-grid-snake.svg"
          curl -s "https://purge.jsdelivr.net/gh/PankajChauhanji/PankajChauhanji@output/github-contribution-grid-snake-dark.svg"
